Fixes a regression from 1.12.0

On devices where both side keys report the same code, 1.12.0 could break a mapping you already had saved. Resolving now falls back to the shared code when nothing is mapped to the exact button, so "both buttons do the same thing" keeps working — and assigning one button individually still overrides it for just that button.
